var validation = (function () {

    //initializing modul
    var init = function () {
        _setUpListeners();
    };

    //listen events
    var _setUpListeners = function () {

    };
    //return object (public method)
    return {
        init: init
    };

})();

validation.init();
